import React from "react";
import styles from "./masthead.module.scss";

const Masthead = () => {

    return (
        <div className={`flex ${styles.container}`}></div >
    );
}

export default Masthead;